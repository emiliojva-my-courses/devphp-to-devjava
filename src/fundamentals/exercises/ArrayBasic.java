package fundamentals.exercises;

/**
 * Exercise 2: Basic Array in Java
 * <p>
 * - Arrays em java são usados para armazenar múltiplos valores do mesmo tipo em uma única variável.
 * - Eles são de tamanho fixo, o que significa que você deve definir o tamanho do array no momento da criação.
 * - A indexação dos arrays começa em 0, ou seja, o primeiro elemento
 * está na posição 0, o segundo na posição 1, e assim por diante.
 * - Arrays podem ser unidimensionais (uma única linha) ou multidimensionais (como matrizes).
 * - Você pode acessar e modificar os elementos do array usando seus índices.
 * - Arrays são úteis para armazenar coleções de dados relacionados.
 * </p>
 */
class ArrayBasic {
    public static void main(String[] args) {

        // esse é meu primeiro contato com arrays desde que sai do PHP

        // array unidimensional
        int[] idades = {3, 5, 40, 44};

    }
}
